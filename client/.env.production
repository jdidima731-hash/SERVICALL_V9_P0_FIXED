# ✅ FIX SÉCURITÉ: Ne jamais committer une vraie clé ici.
# Injecter VITE_ENCRYPTION_KEY à build-time via les variables d'environnement CI/CD.
# Exemple: VITE_ENCRYPTION_KEY=$(openssl rand -hex 16) pnpm build
VITE_ENCRYPTION_KEY=CHANGE_ME_inject_at_build_time
